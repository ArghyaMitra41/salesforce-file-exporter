# Optional: pre-bake a default Consumer Key so users don't have to enter it
VITE_SF_DEFAULT_CLIENT_ID=

# Set to your deployment URL (used for the redirect URI and setup guide)
VITE_APP_URL=https://your-app.vercel.app
